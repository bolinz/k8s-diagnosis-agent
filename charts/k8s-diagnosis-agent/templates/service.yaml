apiVersion: v1
kind: Service
metadata:
  name: k8s-diagnosis-agent
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: k8s-diagnosis-agent
  ports:
    - name: http
      port: 8080
      targetPort: http
