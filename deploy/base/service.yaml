apiVersion: v1
kind: Service
metadata:
  name: k8s-diagnosis-agent
  namespace: k8s-diagnosis-system
spec:
  selector:
    app: k8s-diagnosis-agent
  ports:
    - name: http
      port: 8080
      targetPort: http
