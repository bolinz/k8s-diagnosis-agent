# v0.6.0 (Planned)

Release focus: diagnosis trustworthiness and explainability.

## Day 1 Deliverables

- Freeze new report fields for quality and explainability.
- Add backward-compatible schema scaffolding in service/formatter/CRD.
- Keep runtime behavior unchanged until Day 2+ logic lands.

## New Status Fields (additive)

`DiagnosisReport.status` adds:

- `qualityScore: object`
- `uncertainties: string[]`
- `evidenceAttribution: object[]`

### Intended semantics

- `qualityScore`
  - Holds diagnosis quality metadata (for example overall score and dimensions).
  - Day 1 default: empty object `{}`.
- `uncertainties`
  - Explicit uncertainty statements for operators.
  - Day 1 default: empty list `[]`.
- `evidenceAttribution`
  - Evidence-to-source mapping (tool, timestamp, signal, objectRef).
  - Day 1 default: empty list `[]`.

## Compatibility Contract

- No breaking changes to existing fields.
- Old reports without new fields must still render and be queryable.
- API responses normalize absent fields to defaults:
  - `qualityScore = {}`
  - `uncertainties = []`
  - `evidenceAttribution = []`

## CRD Strategy

- Keep fields optional and additive under `status`.
- Use `x-kubernetes-preserve-unknown-fields` for `qualityScore` and attribution objects to allow iterative shape refinement in v0.6.x.

## Implementation Notes

- `agent/models.py`:
  - `DiagnosisResult` extended with optional fields and safe defaults.
- `agent/reporting/diagnosis_reporter.py`:
  - Always writes the 3 new status fields.
- `agent/service.py`:
  - Normalizes and exposes fields in list/detail API output.
- `deploy/base/crd.yaml`:
  - Adds schema entries for the 3 fields.

## Day 2+ Follow-ups

- Day 2: quality scoring engine and rule-based dimensions.
- Day 3: evidence attribution population from tool call records and timeline signals.
- Day 4: UI rendering for score, uncertainties, and evidence-source drilldown.

## Progress Snapshot

- Day 2 completed:
  - deterministic `qualityScore` (`overall`, per-dimension scores, `method`, `usedFallback`)
  - uncertainty generation for fallback and weak-evidence cases
- Day 3 completed (backend):
  - `evidenceAttribution` auto-population from trace tool sequence
  - timeline-signal attribution from `evidenceTimeline`
  - trigger-signal attribution for event-based diagnoses
- Day 4 completed (frontend):
  - detail page renders `Diagnosis Quality` panel (overall, method, fallback, dimension scores)
  - detail page renders `Uncertainties` panel (explicit operator caveats)
  - attribution view renders `Evidence Attribution` list (source/signal/tool/object/time)
  - old reports remain compatible when new fields are missing
- Day 5 baseline completed (cluster e2e):
  - `scripts/e2e/run_complex_failure.sh` passed on current cluster
  - verified active Ollama diagnosis loop and report writes in pod logs
  - noted rollout gate: cluster is still on `analysisVersion=0.5.1`, so strict v0.6 explainability checks are pending image upgrade

## Day 4 Validation

- Frontend unit tests:
  - `cd web && npm run test`
- Frontend build:
  - `cd web && npm run build`
- HTTP UI regression:
  - `python3 -m pytest -q tests/test_http_ui.py`

Expected checks:

- `Diagnosis Quality` card is visible in report detail.
- `Uncertainties` card is visible and does not crash on empty data.
- `Evidence Attribution` card is visible in `all` and `attribution` views.
- Legacy reports without `qualityScore/uncertainties/evidenceAttribution` still render normally.

## Day 5 Validation

- Baseline complex-failure run:
  - `WORKLOAD_NS=diag-e2e REPORT_NS=k8s-diagnosis-system WAIT_SECONDS=180 ASSERT_WAIT_SECONDS=420 ASSERT_INTERVAL_SECONDS=30 scripts/e2e/run_complex_failure.sh`
- Strict v0.6 explainability check (after rollout):
  - `python3 scripts/e2e/assert_v060_explainability.py --report-namespace k8s-diagnosis-system --workload-namespace diag-e2e`
